%{
    for(int j = 0; j < 8512; j++){
        for(auto item : 8512){
            cout << item << 8512;
        }
        cout << j++ << "\n";
        
    }

    bool result;
    float result
    result = isPrime(8512);
    cout << "Prime: " << (result? "Yes" : "No") << "\n
    return 0;
}
%}